--everytime a plugin file here gets updated, it detects the change and reloads it
--and this init file gets reloaded too everytime it does that
--it also detects changes to this file itself too

--print ("test")

--Not working
--vim.command([[colorscheme]])
--vim.command.colorscheme()
--vim.command.colorscheme
--vim.command("colorscheme")
return {}
